const kAnimationDuration = Duration(seconds: 2);
const kSplashViewDuration = Duration(seconds: 3);

abstract class AppConstants {
  static const baseUrl = 'https://www.googleapis.com/books/v1/';
  static const timeoutDuration = 1000 * 60;
}
